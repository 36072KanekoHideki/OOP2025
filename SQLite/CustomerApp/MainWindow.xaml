<Window x:Class="CustomerApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomerApp"
        mc:Ignorable="d"
        Title="顧客管理アプリケーション" Height="430" Width="1000"
        Background="#FCF5E8">

    <Window.Resources>
        <local:ByteArrayToImageConverter x:Key="ImageConverter"/>

        <Style x:Key="PlaceholderTextBoxStyle" TargetType="TextBox">
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Background="{TemplateBinding Background}"
                        CornerRadius="3">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"/>
                                <TextBlock x:Name="Placeholder"
                                   Text="検索キーワードを入力"
                                   Foreground="Gray"
                                   Margin="5,0,0,0"
                                   VerticalAlignment="Center"
                                   IsHitTestVisible="False"
                                   Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Margin="10">
            <StackPanel>
                <Grid Grid.IsSharedSizeScope="True">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition SharedSizeGroup="LabelGroup"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="名前" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,5,0"/>
                        <TextBox x:Name="NameTextBox" Grid.Column="1"/>
                    </Grid>
                    <Grid Grid.Row="1" Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition SharedSizeGroup="LabelGroup"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="電話番号" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,5,0"/>
                        <TextBox x:Name="PhoneTextBox" Grid.Column="1"/>
                    </Grid>
                    <Grid Grid.Row="2" Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition SharedSizeGroup="LabelGroup"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                    </Grid>
                    <Grid Grid.Row="3" Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition SharedSizeGroup="LabelGroup"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="住所" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,5,0"/>
                        <TextBox x:Name="AddressTextBox" Grid.Column="1"/>
                    </Grid>
                </Grid>
                <GroupBox Header="画像プレビュー" Margin="0,10,0,0">
                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="5" Background="#FFF" Width="150" Height="150">
                        <Image x:Name="SelectedImage" Stretch="Uniform"/>
                    </Border>
                </GroupBox>
            </StackPanel>
            <StackPanel>
                <Button Content="登録" x:Name="SaveButton" Margin="0,10,0,10" Height="30" Click="SaveButton_Click"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <StackPanel Orientation="Vertical">
                    <Button Content="削除" x:Name="DeleteButton" Margin="0,0,0,5" Click="DeleteButton_Click"/>
                    <Button Content="更新" x:Name="UpdateButton" Margin="0,0,0,0" Width="160" Click="UpdateButton_Click"/>
                </StackPanel>
                <Button Content="画像" x:Name="Picture" Margin="5,0,0,0" Width="140" Click="Picture_Click"/>
            </StackPanel>
        </StackPanel>
        <Grid Grid.Column="2">
            <StackPanel Grid.Column="1" Margin="10">
                <TextBox x:Name="SerchTextBox"
         FontSize="16"
         Height="30"
         Margin="0,0,0,10"
         VerticalAlignment="Top"
         TextChanged="SerchTextBox_TextChanged"
         Style="{StaticResource PlaceholderTextBoxStyle}"/>
                <Grid Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="200"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="画像" Grid.Column="0" FontWeight="Bold" Foreground="#555" HorizontalAlignment="Left"/>
                    <TextBlock Text="名前" Grid.Column="1" FontWeight="Bold" Foreground="#555" HorizontalAlignment="Left"/>
                    <TextBlock Text="電話番号" Grid.Column="2" FontWeight="Bold" Foreground="#555" HorizontalAlignment="Left"/>
                    <TextBlock Text="住所" Grid.Column="3" FontWeight="Bold" Foreground="#555" HorizontalAlignment="Left"/>
                </Grid>
                <ListView x:Name="CustomerListView"
          Height="300"
          ScrollViewer.VerticalScrollBarVisibility="Auto"
          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#CCC" BorderThickness="0,0,0,1" Padding="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="200"/>
                                    </Grid.ColumnDefinitions>
                                    <Image Grid.Column="0"
                                           Width="50"
                                           Height="50"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Source="{Binding Picture, Converter={StaticResource ImageConverter}}"
                                           Stretch="UniformToFill"
                                           Margin="5"/>
                                    <TextBlock Text="{Binding Name}" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="16" Foreground="#333"/>
                                    <TextBlock Text="{Binding Phone}" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Left" Foreground="#007ACC"/>
                                    <TextBlock Text="{Binding Address}" Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Left" Foreground="#2E8B57"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Grid>
    </Grid>
</Window>

